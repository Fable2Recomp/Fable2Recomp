# Add CPU source files
set(CPU_SOURCES
    # Add CPU source files here
)

set(CPU_HEADERS
    # Add CPU header files here
)

# Add sources to main target
target_sources(fable2 PRIVATE ${CPU_SOURCES} ${CPU_HEADERS}) 